# Local-only working notes and PM-side spec docs, not repo artifacts.
context/
.claude/*
# ship only the committed alpha-test skill; keep the rest of .claude/ local
!.claude/skills/
.claude/skills/*
!.claude/skills/alpha-test/
.uveddi/

# Added by cargo
/target
# Local task/spec assignment files, not repo artifacts
/assignments
# Local dev .agent/ stores (examples/ carry their own fixtures)
/.agent/

# Per-project daemon state (config.toml, state.json); local-only
/.agent/.dreamd/

# dreamd-mcp is a zero-dependency npx shim; its npm lockfile is noise, and only
# package.json + the pinned manifest.json define what ships (see files[] there).
packages/dreamd-mcp/package-lock.json
